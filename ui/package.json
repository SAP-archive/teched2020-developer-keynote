{
  "name": "developer-keynote-analytics",
  "version": "1.0.0",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "start": "node node_modules/@sap/approuter/approuter.js",
    "build": "npx rimraf dashboard.zip && cd dashboard/ && npx bestzip ../dashboard.zip *"
  },
  "dependencies": {
    "@sap/approuter": "^9.0.1"
  }
}
